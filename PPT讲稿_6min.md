# PPT 讲稿 · 6 分钟（可直接照念）

> 全篇约 1600 字，按 230 字 / 分钟计约 6 分 25 秒。
> 同样的内容已经逐页写进 PPT 的「备注」栏，放映时用演讲者视图能直接看到。
> 方括号里是动作提示，不用念。每页翻页前留半秒停顿。

---

## 封面 — 20 秒

我讲的项目是 IPMaster-Cowork 的后端服务，一个 Agent Runtime。

代码在 `app` 目录下，150 个模块，一万六千九百多行 Python，27 组测试。

今天不讲产品功能，讲这套代码是怎么组织的。

[翻页]

---

## 01 两类现有形态的局限 — 60 秒

现在做 Agent 系统主要是两种写法。

第一种是 Workflow 型，节点和连边在开发期定义好。问题是，新增一条分支就要改图、改代码、重新发布；节点粒度也是固定的，运行期不能再拆分或者增派执行者；异常路径还得事先穷举，组合数随节点数往上涨。

第二种是 ReAct 单循环，thought、action、observation 累积在一条消息链上。它灵活，但没有 task 对象——依赖关系、执行进度、单独重试，这些都无从表达；消息链单调增长，长流程必然顶到上下文上限；失败了整段重来，任务做没做完是模型自己在正文里说的，工具白名单也是全局一套。

我们的做法是两边各取一半。

[指下方卡片]

执行部分沿用 ReAct 循环：AgentLoop 每轮 reason、act、observe，下一步调什么工具由模型在执行中决定，计划可以中途补拆，不需要预先把流程画完。

约束部分靠 task 对象和复核：每一步落成 task，带 status、dag_deps 和 agent 归属；Observer 每轮提交评估，判完成的 task 还能回退到 PENDING 重做，失败按 failure_counter 计数重试。

[翻页]

---

## 02 app/ 目录构成 — 30 秒

这是代码的实际分布。

runtime 最重，3087 行，是执行层；然后是 tools 2198 行、llm 1841 行、domain 1818 行。

调度层 orchestrator 1806 行，比执行层轻——因为调度只做决策，不做具体的事。

右边是目录结构，目录名基本就是能力名，domain 放领域模型和状态机，orchestrator 放三个管理者和任务队列，runtime 放执行链，下面三个是模型、工具、技能三类能力接入。

[翻页]

---

## 03 分层与依赖方向 — 45 秒

四层，依赖自上而下，跨模块通知走 EventBus。

API 层，路由按资源划分，只做协议转换和参数校验，执行过程通过 SSE 推给前端。Service 实例集中在 `deps.py` 里用 `lru_cache` 装配成单例，再注入路由。

Orchestrator 层三个管理者：SessionManager 建会话和 root agent 并启动执行线程，TaskManager 订阅任务事件、维护队列、决定下一个跑谁，LifecycleManager 维护 agent 树，复用或者新建执行体。

Runtime 层是 AgentLoop，按 Reasoner、Actor、Observer 三段执行单个 task，工具调用收口到 ToolGateway，权限过 PolicyEngine。

这里有一条我想强调：**runtime 目录下没有任何一个 provider 的 import**。换模型、加 MCP server，执行链路一行都不用改。

[翻页]

---

## 04 Session / Task / Agent 与三个 Manager — 45 秒

复杂度是按状态归属拆的。

Session 是一次会话的生命周期，由 SessionManager 管，token 预算的校验也在这里。

Task 是一个可执行单元，由 TaskManager 管，队列的三个 push 点集中在它这儿，重试和会话终结也是它决定。

Agent 是一个执行体实例，由 LifecycleManager 管，维护 agent 树，决定复用还是新建，以及子 agent 继承哪些 memory。

[指左下]

左下角是任务队列的结构：`_ready` 是 LIFO 栈，依赖已满足；`_blocked` 是集合，依赖没满足。某个任务终结后，把 blocked 里依赖已满足的挑进 ready。队列还能序列化，随 session 持久化，进程重启后调度状态还在。

右下角列的是各类状态的写入方——谁能改什么，代码里是固定的。

[翻页]

---

## 05 AgentLoop 执行链 — 60 秒

这是最核心的一段代码，一次调用执行一个 task。

Reasoner 组装 memory、blackboard、agent 身份和项目背景，检索可用的工具与技能，必要时触发上下文压缩，输出 ReasoningContext。

Actor 拿着它构建 prompt，流式调用模型，经 PolicyEngine 授权后执行工具和技能，逐轮累积，输出 ActorResult。

Observer 以 ReAct 循环评估这一轮，提交任务评估与复核，写 memory 摘要并发布事件。

[指右上代码]

右边是 run 方法的实际代码。主干就是 reason、act、observe 三行。

中间这个 SUSPENDED 分支要单独说一句：这是等人回答的出口。人工介入在这里不是旁路逻辑，是这条链路上的一个正常返回点——写完本轮记录就退出，用户回答之后 task 重新入队，下一轮从头再跑一遍。

[指右下]

run 方法自己不拼 prompt、不调模型、不执行工具、也不判定任务结果，它只管这几步的先后和退出路径。三个阶段之间只传 `types.py` 里的三个 dataclass，不共享可变状态——所以 Observer 可以拿一个构造好的 ActorResult 单独测。

[翻页]

---

## 06 一个 task 的完整路径 — 40 秒

把刚才那段放到完整链路里看一遍。

[从左上顺着箭头走]

task 创建后发 TASK_CREATED，TaskManager 订阅到就入队：依赖满足的进 `_ready` 栈，没满足的进 `_blocked`。pop 取栈顶，是 LIFO，所以刚拆出来的子任务先做完，不会横着铺开一堆半成品。

状态转成 ACTIVE 之后问 LifecycleManager 要执行者，复用还是按模板新建由 task 属性决定，然后才进 AgentLoop 的三段。

[指中间 alt 那块]

这一块是 SUSPENDED 分支，等人回答时写完记录就退出，回答之后重新入队，下一轮从 run 重新开始。

[指底部]

最后状态经转换表校验落库，事件发回 TaskManager，notify_completed 把依赖已经满足的 task 放行，再 pop 下一个；队列空了，这次会话就结束。

整条路径没有一处流程是预先定义好的，但每一步都留下了对象和状态。

[翻页]

---

## 07 五处扩展点 — 40 秒

会变的地方集中在五处。

LLM Adapter，变化点是模型供应商。基类是抽象类，加一家供应商等于写一个 adapter 文件加注册一行。

Registry，变化点是系统支持哪些能力。工具、技能、模型、agent 模板四类注册表，注册一次，调用方不变。

Tool Gateway，变化点是工具来源。内置、控制、MCP 三类走同一条路径：授权、审计、执行、再审计。

State Machine，变化点是状态与恢复路径。Session、Task、Agent 各一张转换表，集中校验，非法转换直接抛错。

EventBus，变化点是一件事发生后要做几件事。领域服务只管 publish，消费方 subscribe 注册，加一个消费者生产方不用动。

这五处都是实际迭代中反复改动之后才抽出来的。

[翻页]

---

## 08 代码质量六项 — 35 秒

这一页对着评分项，每一条都能翻到具体文件。

可读：类名和目录名对应领域概念，调度、执行、能力三层的命名一一对应到模块。

可维护：能力变更不进入 runtime，新增 provider 等于实现基类加注册一次。

可测试：外部依赖经接口注入，MockAdapter 顶掉真实模型，27 组测试不访问网络。

安全：工具调用单一入口，授权、审计、参数脱敏、输出截断都集中在 `ToolGateway.call()`。

可靠：状态转换集中校验，非法转换抛 INVALID_STATE_TRANSITION，挂起可恢复，失败按计数重试。

可演进：四类注册表，启动时装配。

[翻页]

---

## 09 小结 — 25 秒

三句话收一下。

结构上，按状态归属拆对象，执行链拆成固定阶段，变化点收敛到 adapter、registry、gateway 三处。

工程上，依赖集中装配，规则在构造期建索引，横切逻辑收口到单一入口，队列可序列化恢复。

语言上，Protocol、dataclass、Literal、延迟注解、threading 原语，这些用法在仓库里都有出处。

同样的拆法换成任务调度或者工作流引擎也成立，对象名字变了，阶段和扩展点的位置不变。

我的部分讲完了，谢谢。

---

## 计时卡

| 页 | 内容 | 时长 | 累计 |
|---|---|---|---|
| 封面 | 项目与规模 | 0:20 | 0:20 |
| 01 | 两类形态的局限 + 取法 | 1:00 | 1:20 |
| 02 | 目录构成 | 0:25 | 1:45 |
| 03 | 分层与依赖 | 0:45 | 2:30 |
| 04 | 三个 Manager | 0:40 | 3:10 |
| 05 | AgentLoop 执行链 | 0:55 | 4:05 |
| 06 | 一个 task 的完整路径（时序图） | 0:40 | 4:45 |
| 07 | 五处扩展点 | 0:40 | 5:25 |
| 08 | 代码质量六项 | 0:35 | 6:00 |
| 09 | 小结 | 0:25 | 6:25 |

附录 A1–A5 五页放映时跳过，被问到再翻，每页左侧三条就是要讲的点。

**超时应急**：06 页五条砍成三条（Adapter、Gateway、EventBus），07 页只念安全、可靠、可测试三条，能省下 40 秒。

**被打断提问后**：直接跳到 05、06 两页讲完执行链，其余页略过，保证核心讲到。
