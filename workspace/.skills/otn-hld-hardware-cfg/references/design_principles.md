## 设计原则

**原则1：设备和单板同步更新**

B机房配置需同时完成设备级和单板级的升级替换，不允许只换单板不换设备框，或只换设备框保留旧单板。

**原则2：1:1数量更新**

B机房每种单板的数量与A机房保持一致，不缩减也不扩容。本次搬迁目标是平移业务，容量规划不在本次范围内。

**原则3：EOS设备强制替换**

生命周期状态为 EOS 的设备必须替换为集采目录中的对应设备：

- EOS 设备的单板 → 在集采设备中按单板短名称精确匹配替换（算法详见 `merge_algorithm.md` 阶段三）
- GA 设备 → 保留原设备类型和单板型号，无需变更
