## 设计原则

### 原则一：剔除未使用槽位

**状态**列为"未使用"的单板，不得带入B机房配置表。空槽位和闲置单板一律排除。

### 原则二：EOX单板 1:1 替换

**EOM列**为"Y"的单板，须按中国移动集采框架替换为对应新型号：

- 以单板的 **BomCode** 在替换对照表中查找
- 用对照表中的新 **BomCode** 和 **Description** 替换原值
- **槽位号、子槽位（Card）、Type、状态保持不变**
- 若为EOX单板但在对照表中未找到替换型号，须在输出中显式标注：`⚠️ 未找到替换型号，请人工确认`

