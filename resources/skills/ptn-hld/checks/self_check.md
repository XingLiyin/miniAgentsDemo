## 自校验

生成完成后，逐条核查以下清单。**有任意一项不满足，则重新推理并重新生成：**

- [ ] A机房配置表只含 LPU 类型单板，不含控制板、交叉板等管控类单板
- [ ] B机房配置表的槽位号与A机房逐行一致，无增删槽位
- [ ] 若设备无需 EOX 替换（`{{NEEDS_REPLACE}}` = false），A/B 机房配置表内容完全一致
- [ ] 若设备需要 EOX 替换（`{{NEEDS_REPLACE}}` = true），B机房设备类型已替换为 `{{NEW_DEVICE_TYPE}}`
- [ ] B机房每块单板型号均来自 `{{CANDIDATES}}` 列表，不得自行编造型号
- [ ] B机房每块单板端口数 ≥ 对应A机房槽位的端口数（不得缩容）
- [ ] 有任何端口数不匹配的单板，已标注 `⚠️ 端口数不匹配，请人工确认`
- [ ] 输出中不含任何未替换的占位符（如 `{{ROOM_A_TABLE}}`、`{{ROOM_B_TABLE}}`、`{{HARDWARE_CHANGE}}`）
